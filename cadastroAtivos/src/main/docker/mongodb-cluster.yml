version: '2'
services:
  cadastroativos-mongodb:
    image: mongo:4.0.13
    ports:
      - '27017:27017'
    command: mongos --configdb cadastroativos-mongodb-config
  cadastroativos-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  cadastroativos-mongodb-config:
    image: mongo:4.0.13
    container_name: cadastroativos-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db

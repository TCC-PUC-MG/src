version: '2'
services:
  segcomunicacao-mongodb:
    image: mongo:4.0.13
    ports:
      - '27017:27017'
    command: mongos --configdb segcomunicacao-mongodb-config
  segcomunicacao-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  segcomunicacao-mongodb-config:
    image: mongo:4.0.13
    container_name: segcomunicacao-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
